
{
  "name": "project-storm",
  "version": "1.0.0",
  "private": true,
  "dependencies": {
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "react-router-dom": "^7.1.0",
    "lucide-react": "^0.470.0",
    "framer-motion": "^11.0.0",
    "recharts": "^2.15.0"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  },
  "scripts": {
    "build": "esbuild index.tsx --bundle --outfile=build/index.js --format=esm --minify --external:react --external:react-dom --external:react-router-dom --external:lucide-react --external:recharts --external:framer-motion && cp index.html metadata.json build/ && cp public/*.png build/ && sed -i 's/index.tsx/index.js/g' build/index.html"
  }
}

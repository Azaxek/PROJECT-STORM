
{
  "name": "project-storm",
  "version": "1.0.0",
  "private": true,
  "dependencies": {
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "react-router-dom": "^7.1.0",
    "lucide-react": "^0.470.0",
    "framer-motion": "^11.0.0",
    "recharts": "^2.15.0"
  },
  "scripts": {
    "build": "mkdir -p build && cp index.html index.tsx App.tsx metadata.json build/ && cp -r pages components build/ 2>/dev/null || true"
  }
}
